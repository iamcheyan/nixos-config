#!/usr/bin/env bash
echo 'This locally managed machine has no Nixarchy factory-reset service.'
echo 'Use NixOS generation rollback or review root/home Snapper snapshots with nixos-update list.'
echo 'See ~/nixos-config/docs/nixos-update-snapshots.md before restoring data.'
exit 1
