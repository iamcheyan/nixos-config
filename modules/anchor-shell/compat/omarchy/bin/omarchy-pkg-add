#!/usr/bin/env bash
echo 'This machine is managed by ~/nixos-config.'
echo 'Edit repository modules for packages/services; use chezmoi for private user preferences.'
echo 'Review the diff, then: sudo nixos-rebuild switch --impure --flake ~/nixos-config#hx90'
echo 'Ask your agent to read the globally installed nixos skill for the relevant workflow.'
