# nix build result symlink (nixos-rebuild / nix build artifact)
/result

# Keep the local extension directory itself and its placeholder tracked.
# Everything else under local/ is machine-specific and ignored.
/local/*
!/local/
!/local/README.md
